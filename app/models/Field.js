export default class Field {
    constructor(top, left, bottom, right, hold) {
        this.top = top;
        this.left = left;
        this.bottom = bottom;
        this.right = right;
        this.hold = hold;
    }
}